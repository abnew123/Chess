package frontend.screens;

import backend.user.User;
import frontend.selectors.AISelector;
import frontend.selectors.GameStartSelector;
import javafx.stage.Stage;

public class GameStartScreen implements Screen {
	public GameStartScreen(Stage myStage, User user) {
		// TODO Auto-generated constructor stub
	}
	private GameStartSelector gameStartSelector;
	private AISelector AISelector;
}
