package frontend.displays;

import javafx.scene.text.Text;
/**
 * current not used, but would act as a full display for a user.
 * not used due to stats display serving the purpose as of now
 * @author shichengrao
 *
 */
public class UserDisplay extends Text{
	public UserDisplay(String text) {
		super(text);
	}
}
